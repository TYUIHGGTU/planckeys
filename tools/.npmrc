# @zmkfirmware 作用域仅在官方 npm 源发布（腾讯等镜像会 403）。
# 其余依赖仍走用户默认 registry（例如公司镜像），只对该作用域做覆盖。
@zmkfirmware:registry=https://registry.npmjs.org/

# 若 Electron 在 pnpm 隔离式 node_modules 下定位二进制异常，可解开下面一行兜底：
# node-linker=hoisted
